# Spark Connector Spec — googlechat
# Auto-generated from OpenClaw extension: googlechat
apiVersion: spark.dev/v1
kind: Connector
metadata:
  id: googlechat
  name: "googlechat"
  type: channel
  version: "2026.4.1-beta.1"
  publisher: "OpenClaw Community"
  category: "Communication"
  tags: ["uchannel"]
  icon: question_answer
  description: >
    OpenClaw Google Chat channel plugin
  source: "https://github.com/openclaw/openclaw/tree/main/extensions/googlechat"
  license: MIT

spec:
  docker:
    image: spark/connector-googlechat
    tag: "2026.4.1-beta.1"

  auth:
    type: api_key

  capabilities:
    supports_threads: true
    supports_reactions: false
    supports_edit: false
    supports_unsend: false
    supports_reply: true
    supports_attachments: true
    supports_images: true
    max_message_length: 4096

  config:
    - name: chat_api
      display: "CHAT_API"
      type: text
      required: true
      description: "Environment variable: CHAT_API"
    - name: chat_certs_url
      display: "CHAT_CERTS_URL"
      type: text
      required: true
      description: "Environment variable: CHAT_CERTS_URL"

  tools:
    []

  versions:
    - version: "2026.4.1-beta.1"
      date: "2026-04-04"
      changes:
        - "Auto-generated from OpenClaw extension"
