# Connector Google Chat

The Connector Google Chat service is an Integration layer connector that bridges the Spark platform with Google Chat. It handles message ingestion from Google Chat spaces, delivers agent responses, and manages Google Chat API event subscriptions and authentication.
