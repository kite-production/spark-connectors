# Spark Connector Spec — Firecrawl
# Generated from OpenClaw extension: firecrawl
apiVersion: spark.dev/v1
kind: Connector

# ── METADATA
metadata:
  id: firecrawl
  name: "Firecrawl"
  type: tool
  version: "2026.4.1-beta.1"
  source: spark
  publisher: "Spark"
  maintainers:
    - name: "Spark Team"
      email: "spark@kite.ai"
  category: "DevTools"
  tags: ["Tool"]
  icon: local_fire_department
  iconColor: "#FF6B35"
  description: >
    OpenClaw Firecrawl plugin
  license: MIT
  homepage: https://github.com/kite-production/spark
  repository: https://github.com/kite-production/spark-connectors

# ── SPEC
spec:
  runtime:
    type: docker
    language: typescript
    docker:
      image: spark/connector-firecrawl
      tag: "2026.4.1-beta.1"
      ports:
        grpc: 50081

  auth:
    type: api_key
    flow: automatic

  ingestion:
    enabled: false
    mode: none
    description: "Action-only — invoked by agents on demand"

  config:
    - name: "webSearch"
      display: "webSearch"
      type: text
      required: false
      description: "webSearch"
      group: connection
    - name: "webFetch"
      display: "webFetch"
      type: text
      required: false
      description: "webFetch"
      group: connection

  tools:
    []

  events:
    []

  health:
    endpoint: "/health"
    interval: 15s
    timeout: 5s
    unhealthy_threshold: 3
    test:
      endpoint: "/test"
      description: "Verify connectivity to Firecrawl"
      timeout: 10s
      expected_status: 200

  dependencies:
    services: ["nats", "control-plane"]

  versions:
    - version: "2026.4.1-beta.1"
      date: "2026-04-05"
      breaking: false
      changes:
        - "Initial Spark marketplace release"
        - "Adapted from OpenClaw firecrawl extension"

  setup: |
    ## Firecrawl Connector Setup
    1. Install the connector from the Spark Marketplace
    2. Create an instance in your workspace
    3. Configure the required parameters
    4. Click 'Test Connection' to verify
    5. Enable the connector for your agents

  code:
    entrypoint: "api.ts"
    files:
      - path: "api.ts"
        language: typescript
        size: 1860
      - path: "index.ts"
        language: typescript
        size: 864
      - path: "openclaw.plugin.json"
        language: json
        size: 1830
      - path: "web-search-provider.ts"
        language: typescript
        size: 87
