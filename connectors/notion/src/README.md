# Connector Notion

The Connector Notion service is an Integration layer connector that bridges the Spark platform with Notion workspaces. It handles page and database synchronization, enabling agents to read, create, and update Notion content through the Notion API.
