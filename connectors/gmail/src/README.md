# Connector Gmail

The Connector Gmail service is an Integration layer connector that bridges the Spark platform with Gmail. It handles email ingestion, delivers agent-composed responses, and manages Gmail API authentication and push notification subscriptions.
